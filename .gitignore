# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# All scraped athlete data — kept local only
data/athletes/        # per-athlete HTML cache (300+ files)
data/exports/         # output parquet/CSV
data/*_guids.json     # club athlete lists (names + PO10 GUIDs)

# Age-grading tables are NOT ignored — committed as a useful data asset
# data/age_grade_tables.json

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/
